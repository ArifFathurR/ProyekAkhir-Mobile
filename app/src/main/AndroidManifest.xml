<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>



    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyekAkhir"
        tools:targetApi="31">

        <receiver android:name=".notifikasi.NotificationReceiver"/>
        <receiver android:name=".notifikasi.BootReceiver"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"/>
        <activity android:name=".ui.dokumentasi.DokumentasiSayaActivity"/>
        <activity android:name=".ui.dokumentasi.TambahDokumentasiActivity"/>
        <activity android:name=".ui.dokumentasi.EditDokumentasiActivity"/>
        <activity android:name=".ui.semua_dokumentasi.DetailDokumentasiActivity"/>
        <activity android:name=".ui.semua_dokumentasi.LihatDokumentasi"/>
        <activity android:name=".ui.semua_dokumentasi.LihatDokumentasiDetail"/>
        <activity android:name=".ui.presensi.Presensi"/>
        <activity android:name=".ui.presensi.IsiPresensiActivity"/>
        <activity android:name=".ui.presensi.HistoryPresensi"/>
        <activity android:name=".ui.semua_dokumentasi.KegiatanSelesai"/>

    </application>

</manifest>